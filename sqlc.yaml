version: "2"
sql:
  - schema: "migrations"
    queries: "internal/warehouse/repository/queries.sql"
    engine: "postgresql"
    gen:
      go:
        package: "repository"
        out: "internal/warehouse/repository"
        sql_package: "pgx/v5"
        emit_json_tags: true